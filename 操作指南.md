# 王明课题组网站 — 操作手册

> 吉林大学化学学院 · 超分子化学 · 纯静态学术网站
> 技术栈：HTML + CSS + JS | 部署：GitHub Pages

---

## 目录

- [一、快速上手（5分钟）](#一快速上手5分钟)
- [二、项目结构](#二项目结构)
- [三、日常操作](#三日常操作)
  - [修改文案](#31-修改界面文案)
  - [管理成员](#32-管理成员)
  - [管理论文](#33-管理论文)
  - [管理新闻](#34-管理新闻)
  - [管理相册](#35-管理相册)
  - [管理合作信息](#36-管理合作信息)
  - [更换横幅/图片](#37-更换横幅图片)
- [四、双语系统](#四双语系统)
- [五、构建脚本](#五构建脚本)
- [六、部署上线](#六部署上线)
- [七、常见问题速查](#七常见问题速查)

---

## 一、快速上手（5分钟）

### 你需要编辑的唯一文件

**90% 的日常操作只需编辑一个文件：**

```
js/data/site-data.js
```

这个文件集中管理了网站的所有内容和数据。修改完刷新浏览器即可看到效果。

### 本地预览

直接在浏览器中打开 `index.html`，或用任意静态服务器：

```bash
npx serve .
```

---

### 实际目录（IDE 中看到的顺序）

```
项目根目录/
├── 404.html                    # 404 页面
├── CLAUDE.md                   # Claude Code 配置
├── README.md                   # 项目说明
├── assets/                     # 静态资源
│   ├── banners/                #  横幅（home.jpg）
│   ├── members/                #  成员照片（中文姓名.jpg）
│   ├── gallery/                #  相册 ⚠️ 照片待恢复
│   │   ├── lab/               #    课题组活动
│   │   ├── graduation/        #    毕业留念
│   │   │   ├── 2021/
│   │   │   ├── 2022/
│   │   │   ├── 2023/
│   │   │   └── 2025/
│   │   └── academic/          #    学术活动
│   └── papers/
│       ├── pdf/               #  论文PDF（paper-01 ~ paper-60）
│       └── toc/               #  论文封面图（仅15篇有）
├── contact.html                # 联系我们
├── cooperation.html            # 资源与合作
├── css/
│   └── styles.css              # 全局样式
├── gallery.html                # 课题组活动（相册页）
├── index.html                  # 首页
├── js/
│   ├── common.js               # 公共逻辑
│   ├── data/
│   │   └── site-data.js        # ★ 数据中心
│   └── pages/
│       ├── home.js             # 首页渲染
│       ├── members.js          # 成员页渲染
│       ├── publications.js     # 成果页渲染
│       ├── paper.js            # 论文详情渲染
│       ├── news.js             # 新闻列表渲染
│       ├── news-item.js        # 新闻详情渲染
│       ├── gallery.js          # 相册页渲染
│       ├── contact.js          # 联系页（仅语言切换）
│       └── cooperation.js      # 合作页渲染
├── members.html                # 团队成员
├── news-item.html              # 新闻详情（?id=）
├── news.html                   # 新闻列表
├── package.json                # npm 脚本
├── paper.html                  # 论文详情（?id=）
├── publications.html           # 研究成果
├── robots.txt                  # SEO
├── scripts/
│   ├── generate-gallery.mjs   # 扫描 gallery/ → 生成 GALLERY_DATA
│   └── rebuild-all.mjs       # 从 PDF 提取元数据 → 重建 PAPER_DATA
├── sitemap.xml                 # 站点地图
└── 操作指南.md                 # 本文件
```

> **为什么 HTML 都在根目录**：GitHub Pages 要求 `index.html` 在根目录，其他页面若移入子目录需修改所有导航链接，收益不大，因此保持平铺。

### 数据流

```
js/data/site-data.js (全局常量)
    │
    ├── SITE_I18N       ──→ js/common.js (t() / applyLang)
    ├── SITE_NEWS       ──→ home.js / news.js / news-item.js
    ├── PAPER_DATA      ──→ home.js / publications.js / paper.js
    ├── MEMBER_DATA     ──→ members.js
    ├── PI_DATA         ──→ members.js
    ├── GALLERY_DATA    ──→ home.js / gallery.js
    ├── RESEARCH_AREAS  ──→ home.js
    ├── EQUIPMENT_DATA  ──→ cooperation.js
    └── COOPERATION_DATA──→ cooperation.js
```

---

## 三、日常操作

### 3.1 修改界面文案

编辑 `js/data/site-data.js`，找到 `SITE_I18N` 对象（文件开头）。**文案按页面分组嵌套**，例如：

```js
const SITE_I18N = {
  zh: {
    nav: {                        // 导航栏（所有页面共用）
      brand: "王明课题组",
      home: "首页",
      members: "团队成员",
      // ...
    },
    home: {                       // 首页
      progress: "研究进展",
      news: "新闻动态",
      more: "查看更多",
      // ...
    },
    members: {                    // 团队成员页
      banner: "团队成员",
      phd: "在读博士生",
      msc: "在读硕士生",
      pi: "课题组负责人",
      // ...
    },
    publications: {               // 研究成果页
      banner: "研究成果",
      all: "全部",
      featured: "精选论文",
      noPaper: "未找到该论文。",
      // ...
    },
    news: {                       // 新闻页
      banner: "新闻动态",
      category: "分类",
      academic: "学术交流",
      // ...
    },
    gallery: { banner: "课题组活动" },  // 相册页
    contact: {                    // 联系我们页
      welcome1: "对我们的研究感兴趣？欢迎加入或合作！",
      address: "吉林大学唐敖庆楼A404",
      // ...
    },
    cooperation: {                // 资源与合作页
      equipment: "仪器平台",
      partners: "合作伙伴",
    },
    footer: { links: "友情链接" }, // 页脚
  },
  en: { /* 同样结构 */ }
};
```

> **HTML 中**用 `data-i18n="页面.键名"`（如 `data-i18n="members.phd"`）
> **JS 中**用 `t("页面.键名")`（如 `t("publications.noPaper")`）
> 键名用点号分隔，前面是页面分组，后面是具体文案。

---

### 3.2 管理成员

#### 添加新成员

**① 放照片**：将照片放入 `assets/members/`，文件名 = 中文姓名，如 `张三.jpg`

**② 加数据**：编辑 `js/data/site-data.js` 中 `MEMBER_DATA`：

```js
// 在读成员（博士后/博士/硕士）
{ name: "张三", enName: "San Zhang",
  email: "zhangsan@jlu.edu.cn",
  focusZh: "超分子自组装",           // 研究方向（中文）
  focusEn: "Supramolecular Self-Assembly",
  bioZh: "从事……研究。",
  bioEn: "Working on..." }

// 毕业成员（alumni）
{ name: "李四", enName: "Si Li",
  years: "2020-2025",                // 在组时间
  destinationZh: "高校任教",
  destinationEn: "Faculty position" }
```

**③ 分类放入**对应数组：`phd`、`msc`、`postdoc`、`alumni`

#### 修改 PI 信息

编辑 `PI_DATA` 对象：

```js
const PI_DATA = {
  name: "王明",
  enName: "Ming Wang",
  photo: "./assets/members/王明.jpg",
  titleZh: "教授，博士生导师",
  titleEn: "Professor",
  email: "mingwang@jlu.edu.cn",
  bioZh: "……",
  researchZh: "……",
  // ...
};
```

#### 删除成员

直接删除对应条目。照片文件可保留或手动删除。

---

### 3.3 管理论文

#### 方式一：手动添加（推荐，最准确）

在 `PAPER_DATA` 数组中添加：

```js
{
  id: "paper-61",                             // 唯一编号
  date: "2026",                                // 年份
  toc: "./assets/papers/toc/paper-61.jpg",     // TOC图（可选，无则留 ""）
  pdf: "./assets/papers/pdf/paper-61.pdf",     // PDF文件
  zh: {
    title: "论文中文标题",
    journal: "中文期刊名 (英文缩写)",
    authors: "作者1, 作者2, and 王明",
    abstract: "中文摘要……"
  },
  en: {
    title: "English Title",
    journal: "J. Am. Chem. Soc. 2026, 143, 12345–12350",
    authors: "Author1, Author2, and Ming Wang",
    abstract: "English abstract..."
  },
  source: "https://doi.org/10.xxxx/xxxxx",    // DOI链接
  tags: []                                     // 标签：representative / reviews
}
```

**文件放置**：
- PDF → `assets/papers/pdf/paper-61.pdf`
- 封面图 → `assets/papers/toc/paper-61.jpg`（可选）

#### 方式二：批量重建

如果新增了大量论文，可用脚本从 PDF 自动提取元数据：

```bash
# 1. 将 PDF 放入 assets/papers/pdf/
# 2. 将 TOC 图放入 assets/papers/toc/
# 3. 运行
npm run papers
```

> 需要安装 `pdftotext`（[下载 Xpdf](https://www.xpdfreader.com/download.html) 或 `brew install poppler`）。提取效果取决于 PDF 质量。

---

### 3.4 管理新闻

在 `SITE_NEWS` 数组中添加：

```js
{
  id: "news-20260805-1",             // 格式：news-年月日-序号
  date: "2026-08-05",
  tags: ["学术交流"],                 // 论文 / 毕业 / 学术交流 / 获奖 / 网站更新
  zh: {
    title: "某某教授来访交流",
    desc: "简短描述，显示在列表页。",
    detail: [                         // 详情页段落数组
      "第一段内容……",
      "第二段内容……"
    ]
  },
  en: {
    title: "Prof. XX Visited Our Lab",
    desc: "Short description.",
    detail: ["Paragraph 1...", "Paragraph 2..."]
  }
}
```

新闻按 `date` 倒序排列。标签用于新闻列表页的侧边栏筛选。

---

### 3.5 管理相册

#### 添加照片

**① 放文件**：将照片放入 `assets/gallery/` 对应子文件夹：

```
assets/gallery/
├── lab/                  ← 课题组活动照片
├── graduation/
│   ├── 2021/             ← 2021年毕业照
│   ├── 2022/
│   ├── 2023/
│   └── 2025/
└── academic/             ← 学术活动照片
```

**② 重建数据**：

```bash
npm run gallery
```

这个命令扫描目录结构并自动更新 `js/data/site-data.js` 中的 `GALLERY_DATA`。

#### 排序规则

相册页按 `gallery.js` 中 `FOLDER_ORDER` 排序：

```js
var FOLDER_ORDER = ["lab", "graduation", "academic"];
```

如需调整顺序，修改此数组即可。

---

### 3.6 管理合作信息

编辑 `js/data/site-data.js` 中以下常量：

```js
// 仪器平台
const EQUIPMENT_DATA = [
  { zh: "核磁共振波谱仪", en: "NMR Spectrometer" },
  { zh: "……", en: "……" }
];

// 合作伙伴
const COOPERATION_DATA = {
  partners: [
    { zh: "吉林大学化学学院", en: "College of Chemistry, Jilin University" },
  ],
  transfer: {
    zh: ["成果转化描述段落1", "段落2", "段落3"],
    en: ["Transfer paragraph 1", "Paragraph 2", "Paragraph 3"]
  }
};
```

---

### 3.7 更换横幅/图片

#### 首页合照

替换 `assets/banners/home.jpg`（保持同名即可）。

如需更换路径，需同时修改两处：

| 位置 | 文件 |
|------|------|
| HTML 静态 `<img>` | `index.html` 第 74 行 |
| JS 动态轮播数组 | `js/pages/home.js` 第 16 行 `bannerImages` |

> ⚠️ 两处必须一致，否则 JS 会覆盖 HTML 导致显示异常。

#### 子页面横幅

目前子页面横幅图片缺失。如需添加，将图片放入 `assets/banners/`，修改对应 HTML：

| 页面 | 文件 | 建议文件名 |
|------|------|-----------|
| 团队成员 | `members.html` | `assets/banners/members.jpg` |
| 研究成果 | `publications.html` | `assets/banners/publications.jpg` |
| 新闻 | `news.html` | `assets/banners/news.jpg` |
| 相册 | `gallery.html` | `assets/banners/gallery.jpg` |
| 联系我们 | `contact.html` | `assets/banners/contact.jpg` |
| 资源与合作 | `cooperation.html` | `assets/banners/cooperation.jpg` |

#### 成员照片

- 文件名 = 成员中文姓名
- 格式：`.jpg`
- 放置位置：`assets/members/`
- 照片缺失时自动显示姓氏首字作为降级方案

---

### i18n 键 → 网页位置速查

| 键 | 网页上看到的位置 |
|----|-----------------|
| `nav.brand` | 导航栏左上角"王明课题组" |
| `nav.badge` | 导航栏品牌下方小字 |
| `nav.home` ~ `nav.contact` | 导航栏 8 个链接 |
| `home.progress` | 首页"研究进展"区块标题 |
| `home.news` | 首页"新闻动态"区块标题 |
| `home.research` | 首页"研究方向"区块标题 |
| `home.gallery` | 首页"课题组活动"区块标题 |
| `home.more` | 首页"查看更多"链接 |
| `home.quote` | 首页轮播图引述文字 |
| `home.learnMore` | 首页研究方向"了解更多" |
| `members.banner` | 团队成员页横幅 |
| `members.phd` / `msc` / `alumni` / `postdoc` | 成员页分类标题 |
| `members.pi` | PI 卡片"课题组负责人"标签 |
| `members.focus` | PI 卡片"研究方向"标签 |
| `publications.banner` | 研究成果页横幅 |
| `publications.all` | 筛选栏"全部" |
| `publications.representative` | 筛选栏"代表性论文" |
| `publications.reviews` | 筛选栏"综述" |
| `publications.featured` | 精选论文轮播标题 |
| `publications.noPaper` | 论文未找到提示 |
| `publications.summary` / `prev` / `next` / `back` | 论文详情页 |
| `paperDetail.banner` | 论文详情页横幅"研究进展" |
| `news.banner` | 新闻页横幅 |
| `news.category` / `academic` / `awards` / `graduate` | 新闻筛选栏 |
| `news.noNews` / `prev` / `next` / `back` | 新闻详情页 |
| `gallery.banner` | 相册页横幅"课题组活动" |
| `contact.welcome1` / `welcome2` | 联系页欢迎语 |
| `contact.person` / `address` / `postcode` / `email` | 联系页卡片内容 |
| `cooperation.equipment` / `partners` / `transfer` | 合作页三个区块标题 |
| `footer.links` | 页脚"友情链接" |

---

## 四、双语系统

### 工作原理

```
用户点击 "中/EN" 按钮
       │
       ▼
common.js 切换 currentLang（zh ↔ en）
       │
       ├── localStorage.setItem("wm-lab-lang", lang)  ← 持久化
       │
       ├── applyLang()
       │   └── 遍历 [data-i18n] 元素 → 设置文本
       │
       └── 调用页面 refreshFn
           └── 重新渲染动态内容（论文、成员等）
```

### 新增双语文案

**①** 在对应页面分组下，`zh` 和 `en` 中同时添加：

```js
// site-data.js → SITE_I18N
zh: {
  home: {
    newSection: "课题组新动态",   // ← 新增
  }
},
en: {
  home: {
    newSection: "Lab Updates",   // ← 新增
  }
}
```

**②** 在 HTML 中使用（点号路径：`页面.键名`）：

```html
<span data-i18n="home.newSection">课题组新动态</span>
```

**③** 在 JS 中使用：

```js
element.textContent = t("home.newSection");
```

### 新增双语数据

所有数据条目遵循 `zh`/`en` 模式：

```js
{
  zh: { title: "中文", desc: "描述" },
  en: { title: "English", desc: "Description" }
}
```

JS 渲染时用 `data[currentLang]` 自动切换。

---

## 五、构建脚本

| 命令 | 作用 | 前提 |
|------|------|------|
| `npm run gallery` | 扫描 `assets/gallery/`，生成 `GALLERY_DATA` | 无 |
| `npm run papers` | 从 `assets/papers/pdf/` 提取元数据，重建 `PAPER_DATA` | 需 `pdftotext` |
| `npm run build` | 先 gallery 后 papers | |

### 脚本详解

**`generate-gallery.mjs`**
- 读取 `assets/gallery/` 目录树
- 为每张图片生成 `{file, path}` 条目
- 写入 `js/data/site-data.js` 中 `GALLERY_DATA` 段

**`rebuild-all.mjs`**
- 读取 `assets/papers/pdf/` 所有 PDF
- 调用 `pdftotext` 提取英文元数据
- 匹配中英文期刊名对照表
- 输出带 `zh`/`en` 的 `PAPER_DATA`

---

## 六、部署上线

### GitHub Pages（推荐）

```bash
# 1. 初始化 git（如果还没有）
git init
git add .
git commit -m "initial commit"

# 2. 推送到 GitHub
git remote add origin https://github.com/<用户名>/<仓库名>.git
git push -u origin main

# 3. 在仓库 Settings → Pages 中
#    选择分支 main，根目录 /
#    保存后等待 1-2 分钟即可访问
```

### 任意静态服务器

将所有文件上传到 Web 根目录。无需构建步骤，无需 npm install。

### 部署前检查清单

- [ ] `node --check js/data/site-data.js` 语法通过
- [ ] 所有页面 JS 语法通过：`node --check js/pages/*.js`
- [ ] 本地浏览器预览所有页面正常
- [ ] 横幅和照片正常显示
- [ ] 中英文切换正常
- [ ] 论文 DOI 链接可点击

---

## 七、常见问题速查

### 🔴 整站内容空白 / 图片全不显示

**99% 是 site-data.js 语法错误**，导致所有数据未定义。

```bash
node --check js/data/site-data.js
```

最常见原因：
- 字符串跨行（JS 不允许原始换行符在字符串中）
- 转义引号写错（`\"` 少了闭合引号）

### 🟡 首页横幅不显示

`js/pages/home.js` 中 `bannerImages` 数组路径和 `index.html` 中 `<img>` 路径不一致，JS 覆盖了 HTML。确保两处指向同一文件。

### 🟡 某篇论文在列表中不显示

检查 `PAPER_DATA` 中该论文的 `date` 字段是否为空（空日期会被分到独立组放到列表最后）。

### 🟡 按"代表性论文"/"综述"筛选无结果

论文的 `tags` 数组需要包含 `"representative"` 或 `"reviews"`，否则筛选结果为空：

```js
tags: ["representative"]   // 标记为代表作
tags: ["reviews"]          // 标记为综述
```

### 🟡 成员照片显示首字而不是照片

照片文件命名必须与 `MEMBER_DATA` 中的 `name` 字段**精确一致**（包括生僻字）。检查：
1. 文件名是否完全匹配
2. 文件扩展名是否为 `.jpg`
3. 文件是否在 `assets/members/` 目录下

### 🟡 语言切换后内容未更新

检查页面 JS 是否在 `initCommon()` 时传入了刷新回调：

```js
initCommon(renderAll);  // renderAll 会在语言切换时被调用
```

### 🟡 相册轮播不滚动

1. 确认 `GALLERY_DATA` 中有数据
2. 确认 `npm run gallery` 已运行
3. 检查浏览器控制台是否有 JS 错误
